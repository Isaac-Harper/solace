package net.solace.client;

import net.fabricmc.api.ClientModInitializer;

/**
 * Client entrypoint. The native Mod Menu config screen will be wired here
 * alongside the config system (see SPEC.md milestones M4–M5).
 */
public class SolaceClient implements ClientModInitializer {
    @Override
    public void onInitializeClient() {
        // No client behavior yet (M0 scaffold).
    }
}
